#ifndef COMMON_ENUM_HPP
#define COMMON_ENUM_HPP

enum VechicleType
{
    BIKE,
    CAR,
    TRUCK
};

enum SpotType
{
    SMALL,
    COMPACT,
    LARGE
};

enum AccountType
{
    ADMIN,
    OPERATOR,
    USER
};

#endif